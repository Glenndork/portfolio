# Portfolio Redesign Implementation Plan

1. Replace `index.html` with semantic single-page structure: skip link, navigation, hero, four projects, profile/capabilities, contact, and footer.
2. Replace `assets/styles.css` with the Carbon Precision token system, Fontshare typography, responsive layouts, accessible states, and reduced-motion behavior.
3. Replace `assets/script.js` with progressive enhancement for mobile navigation, active-section navigation, and intersection-based project reveals.
4. Validate document structure, local assets and links, JavaScript syntax, responsive layouts, keyboard behavior, reduced motion, and console output.
5. Iterate on any visual or interaction defects found during browser verification.
